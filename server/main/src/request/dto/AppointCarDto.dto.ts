import { CarSubTypesValues, CarTypes } from 'src/car/entities/car.entity';
import { RequestTypes, RequestSubTypes } from '../entities/request.entity';

export class AppointCarDto {
  carId: number;
}
